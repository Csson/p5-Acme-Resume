---
doctype: dist

header:
  name: Acme-Resume

prereqs:
  runtime:
    requires:
      - perl: 5.014000
      - Moops: 0.034
      - Moose: 2.000

plugins:
  - +config: Author::CSSON

---
doctype: weaver

plugins:
  - +config: Author::CSSON

  - +remove_plugin: Splint
